import 'package:get/get.dart';
import 'package:flutter/material.dart';
class ResponsePage extends GetView {
  const ResponsePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("I am response Page"),
    );
  }
}
