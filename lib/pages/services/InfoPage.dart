import 'package:get/get.dart';
import "package:flutter/material.dart";

class InfoPage extends GetView {
  const InfoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
     child: Text("i am info Page")
    );
  }
}
